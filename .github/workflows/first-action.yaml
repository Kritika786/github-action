name: workflow
on: workflow_dispatch
jobs: 
  first_job:
     runs-on: ubuntu-latest
     steps:
        - name: print greeting
          run: |
              echo "Hello hi"
              echo "goodbye"
        - name: Get code
          uses: actions/checkout@v3
          
